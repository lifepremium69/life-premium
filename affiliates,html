<!DOCTYPE html>
<html lang="en" class="dark">
<head>
<!-- Google tag (gtag.js) -->
<script async src="https://www.googletagmanager.com/gtag/js?id=G-NC59DJ9CPD"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());

  gtag('config', 'G-NC59DJ9CPD');
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Become a Premium Shiller™ – Life Premium</title>
    
    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    
    <!-- Icons -->
    <script src="https://unpkg.com/lucide@latest"></script>

    <!-- Google Fonts: Inter -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">

    <style>
        :root {
            --x-black: #000000;
            --x-card-bg: #16181c;
            --x-gold: #FFD400;
            --x-text-main: #e7e9ea;
            --x-text-dim: #71767b;
            --x-border: #2f3336;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
            background-color: var(--x-black);
            color: var(--x-text-main);
            overflow-x: hidden;
        }

        /* Custom Scrollbar */
        ::-webkit-scrollbar {
            width: 10px;
        }
        ::-webkit-scrollbar-track {
            background: var(--x-black);
        }
        ::-webkit-scrollbar-thumb {
            background: #333;
            border-radius: 5px;
        }
        ::-webkit-scrollbar-thumb:hover {
            background: #555;
        }

        .gold-glow {
            filter: drop-shadow(0 0 20px rgba(255, 212, 0, 0.2));
        }
        
        .text-glow {
            text-shadow: 0 0 30px rgba(255, 212, 0, 0.4);
        }

        /* Fireworks Canvas */
        #fireworksCanvasHeader {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 0;
            opacity: 0.4;
        }
    </style>
</head>
<body class="antialiased selection:bg-[#FFD400] selection:text-black min-h-screen flex flex-col">

    <!-- Sticky Header -->
    <nav class="fixed top-0 w-full z-50 backdrop-blur-md bg-black/70 border-b border-[#2f3336]">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between items-center h-14">
                <!-- Logo -->
                <a href="index.html" class="flex items-center gap-2 cursor-pointer hover:opacity-80 transition">
                    <i data-lucide="crown" class="h-6 w-6 text-white"></i>
                    <span class="font-bold text-lg tracking-tight hidden sm:block">Life Premium™</span>
                </a>
                <!-- Nav Action -->
                <a href="index.html" class="text-sm font-bold text-[#71767b] hover:text-white transition-colors">
                    Back to Reality
                </a>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <main class="flex-grow pt-24 px-4 sm:px-6 pb-20 relative">

        <!-- HEADER SECTION -->
        <div class="max-w-4xl mx-auto text-center mb-20 relative">
            <canvas id="fireworksCanvasHeader"></canvas>
            
            <div class="relative z-10">
                <div class="inline-flex items-center gap-2 px-3 py-1 rounded-full border border-[#FFD400]/30 bg-[#FFD400]/10 text-[#FFD400] text-xs font-bold uppercase tracking-wider mb-6">
                    <i data-lucide="sparkles" class="w-3 h-3"></i> Affiliate Program
                </div>
                
                <h1 class="text-4xl md:text-6xl font-extrabold tracking-tight mb-6 text-white text-glow">
                    Become a Premium Shiller™
                </h1>
                
                <p class="text-xl md:text-2xl text-[#71767b] mb-10 max-w-2xl mx-auto leading-relaxed">
                    Pay $89 once? Earn <span class="text-white font-bold">30% recurring forever</span>. <br>
                    No inventory, just memes + money.
                </p>
            </div>
        </div>

        <!-- BENEFITS GRID -->
        <div class="max-w-6xl mx-auto mb-24">
            <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
                
                <!-- Benefit 1 -->
                <div class="bg-[#16181c] p-8 rounded-3xl border border-[#2f3336] hover:border-[#FFD400] transition-all group">
                    <div class="w-12 h-12 rounded-full bg-[#FFD400]/10 flex items-center justify-center mb-6 group-hover:bg-[#FFD400] transition-colors">
                        <i data-lucide="megaphone" class="w-6 h-6 text-[#FFD400] group-hover:text-black"></i>
                    </div>
                    <h3 class="text-xl font-bold text-white mb-3">Clout Multiplier</h3>
                    <p class="text-[#e7e9ea] leading-relaxed">
                        Flex your cert on X, tag us for retweets. Instant engagement farming.
                    </p>
                </div>

                <!-- Benefit 2 -->
                <div class="bg-[#16181c] p-8 rounded-3xl border border-[#2f3336] hover:border-[#FFD400] transition-all group">
                    <div class="w-12 h-12 rounded-full bg-[#FFD400]/10 flex items-center justify-center mb-6 group-hover:bg-[#FFD400] transition-colors">
                        <i data-lucide="banknote" class="w-6 h-6 text-[#FFD400] group-hover:text-black"></i>
                    </div>
                    <h3 class="text-xl font-bold text-white mb-3">Passive Income</h3>
                    <p class="text-[#e7e9ea] leading-relaxed">
                        <span class="text-[#FFD400] font-bold">$26.70/year</span> per referral (recurring). Do the math. It adds up.
                    </p>
                </div>

                <!-- Benefit 3 -->
                <div class="bg-[#16181c] p-8 rounded-3xl border border-[#2f3336] hover:border-[#FFD400] transition-all group">
                    <div class="w-12 h-12 rounded-full bg-[#FFD400]/10 flex items-center justify-center mb-6 group-hover:bg-[#FFD400] transition-colors">
                        <i data-lucide="layout-dashboard" class="w-6 h-6 text-[#FFD400] group-hover:text-black"></i>
                    </div>
                    <h3 class="text-xl font-bold text-white mb-3">Easy Tools</h3>
                    <p class="text-[#e7e9ea] leading-relaxed">
                        Custom links, analytics dashboards, auto-payouts via PayPal. Minimal effort.
                    </p>
                </div>

            </div>
        </div>

        <!-- COPY / SIGNUP SECTION -->
        <div class="max-w-4xl mx-auto mb-12 text-center">
            <h2 class="text-3xl font-bold mb-6 text-white">Upgrade Your Hustle</h2>
            <p class="text-lg text-[#71767b] leading-relaxed mb-12">
                Tired of average referrals? People buy the joke – you cash the check. <br>
                Join the only affiliate program that officially validates your existence.
            </p>
            
            <!-- FirstPromoter Embed -->
            <div class="bg-[#16181c] rounded-3xl border border-[#2f3336] overflow-hidden shadow-2xl shadow-[#FFD400]/5">
                 <iframe src="https://yourcampaign.firstpromoter.com/signup" width="100%" height="600" frameborder="0"></iframe>
            </div>

            <div class="mt-10">
                <button class="px-10 py-4 rounded-full bg-[#FFD400] text-black font-extrabold text-lg hover:bg-[#ffdf40] transition-all transform hover:scale-105 shadow-lg shadow-[#FFD400]/20">
                    Join Now – First 100 Get Bonus Cert Badge
                </button>
            </div>
        </div>

    </main>

    <!-- FOOTER -->
    <footer class="max-w-3xl mx-auto w-full text-center border-t border-[#2f3336] pt-10 pb-10 px-4">
        <p class="text-[#536471] text-xs leading-relaxed italic mb-6">
            Satirical earnings. No guarantees except laughs. Don't spend it all in one place (unless it's here).
        </p>
        <div class="flex justify-center gap-6 text-sm text-[#71767b]">
            <a href="index.html" class="hover:text-white transition-colors">Home</a>
            <a href="#" class="hover:text-white transition-colors">Terms</a>
            <span>© 2025 Life Corp.</span>
        </div>
    </footer>

    <script>
        lucide.createIcons();

        // --- FIREWORKS ANIMATION ENGINE (Light version for header) ---
        (function() {
            const canvas = document.getElementById('fireworksCanvasHeader');
            const ctx = canvas.getContext('2d');
            let width, height;
            let particles = [];
            let fireworks = [];
            const colors = ['#FFD400', '#FFFFFF', '#333333'];

            function resize() {
                const parent = canvas.parentElement;
                width = parent.clientWidth;
                height = parent.clientHeight;
                canvas.width = width;
                canvas.height = height;
            }
            window.addEventListener('resize', resize);
            resize();

            class Firework {
                constructor() {
                    this.x = Math.random() * width;
                    this.y = height;
                    this.targetX = Math.random() * width;
                    this.targetY = Math.random() * (height * 0.5);
                    this.speed = 3 + Math.random() * 3;
                    this.angle = Math.atan2(this.targetY - this.y, this.targetX - this.x);
                    this.velocityX = Math.cos(this.angle) * this.speed;
                    this.velocityY = Math.sin(this.angle) * this.speed;
                    this.exploded = false;
                }
                update(index) {
                    this.x += this.velocityX;
                    this.y += this.velocityY;
                    const dx = this.targetX - this.x;
                    const dy = this.targetY - this.y;
                    if (Math.sqrt(dx*dx + dy*dy) < this.speed) {
                        createParticles(this.targetX, this.targetY);
                        fireworks.splice(index, 1);
                    }
                }
                draw() {
                    ctx.beginPath();
                    ctx.arc(this.x, this.y, 2, 0, Math.PI * 2);
                    ctx.fillStyle = '#FFD400';
                    ctx.fill();
                }
            }

            class Particle {
                constructor(x, y) {
                    this.x = x;
                    this.y = y;
                    this.angle = Math.random() * Math.PI * 2;
                    this.speed = Math.random() * 2 + 1;
                    this.friction = 0.95;
                    this.gravity = 0.04;
                    this.alpha = 1;
                    this.decay = Math.random() * 0.02 + 0.01;
                    this.color = colors[Math.floor(Math.random() * colors.length)];
                }
                update(index) {
                    this.speed *= this.friction;
                    this.x += Math.cos(this.angle) * this.speed;
                    this.y += Math.sin(this.angle) * this.speed + this.gravity;
                    this.alpha -= this.decay;
                    if (this.alpha <= 0) particles.splice(index, 1);
                }
                draw() {
                    ctx.globalAlpha = this.alpha;
                    ctx.beginPath();
                    ctx.arc(this.x, this.y, 1.5, 0, Math.PI * 2);
                    ctx.fillStyle = this.color;
                    ctx.fill();
                    ctx.globalAlpha = 1;
                }
            }

            function createParticles(x, y) {
                for (let i = 0; i < 20; i++) particles.push(new Particle(x, y));
            }

            function loop() {
                ctx.clearRect(0, 0, width, height);
                if (Math.random() < 0.02) fireworks.push(new Firework());
                for (let i = fireworks.length - 1; i >= 0; i--) { fireworks[i].update(i); fireworks[i]?.draw(); }
                for (let i = particles.length - 1; i >= 0; i--) { particles[i].update(i); particles[i]?.draw(); }
                requestAnimationFrame(loop);
            }
            loop();
        })();
    </script>
</body>
</html>
